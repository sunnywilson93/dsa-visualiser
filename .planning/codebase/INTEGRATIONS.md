# External Integrations

**Analysis Date:** 2026-01-22

## APIs & External Services

**Google Analytics:**
- Service: Google Analytics 4 (GA4)
  - Measurement ID: `G-W2VCY1D7Y7` (hardcoded in `src/components/Analytics.tsx`)
  - Purpose: Track user engagement, page views, interactions on jsinterview.dev
  - Implementation: Google Tag Manager script injection via Next.js `<Script>` component
  - Location: `src/components/Analytics.tsx`
  - Initialization: Automatic page tracking on route changes via Next.js `usePathname()` and `useSearchParams()`

**Google Search Console:**
- Verification: Enabled via metadata in `src/app/layout.tsx`
- Verification Token: `VrYpJpsVJz7IC1u0x6Cwe02I9vd2zk8-k5qgqudMOBw`
- Purpose: SEO verification and search performance monitoring

**Google Fonts:**
- Preconnect: `https://fonts.googleapis.com`
- Purpose: Custom typography (referenced in layout but specific font selection not found in codebase)

## Data Storage

**Databases:**
- Not applicable - No persistent database integration
- All content is static and hardcoded in source

**File Storage:**
- Not applicable - No file upload/download functionality
- Static assets served via Next.js public directory

**Caching:**
- Browser cache: Configured via Next.js headers (default behavior)
- No server-side caching layer (Vercel handles CDN caching automatically)

## Authentication & Identity

**Auth Provider:**
- Not implemented - No user authentication system
- No login/signup functionality
- Application is fully public with no user accounts or sessions

## Monitoring & Observability

**Error Tracking:**
- Not detected - No error tracking service (Sentry, Rollbar, etc.)
- Error messages generated by custom error formatter in `src/engine/errorFormatter.ts`

**Logs:**
- Browser console only (`console.log()` calls tracked in interpreter execution)
- No server-side logging
- No log aggregation service integrated

## CI/CD & Deployment

**Hosting:**
- Vercel (`.vercel/` configuration directory present)
- Production URL: `https://jsinterview.dev`
- OpenGraph metadata points to `https://jsinterview.dev/og-image.png`
- Canonical URL: `https://jsinterview.dev`

**CI Pipeline:**
- Not detected - No GitHub Actions, CircleCI, or other CI service configuration files found
- Vercel automatic deployments likely configured via git integration (default Vercel behavior)

## Environment Configuration

**Required env vars:**
- None required for core functionality
- All configuration hardcoded in source code

**Optional env vars:**
- `.env` and `.env.*` files ignored via `.gitignore` (pattern match present)
- Structure supports future environment variables but none currently used

**Secrets location:**
- No secrets currently stored
- Google Analytics ID is public (embedded in tracking scripts)

## Webhooks & Callbacks

**Incoming:**
- Not applicable - No API endpoints that accept webhooks

**Outgoing:**
- Google Analytics beacon requests sent to `https://www.googletagmanager.com/gtag/js?id=G-W2VCY1D7Y7`
- Page tracking sent to Google Analytics on route changes

## Structured Data & SEO

**JSON-LD Markup:**
Located in `src/app/layout.tsx`:
- Website schema: Identifies site as educational resource
- EducationalOrganization schema: Describes JS Interview Prep as education provider
- SoftwareApplication schema: Documents the app as free web tool
- Purpose: Improved search engine understanding and rich snippet display

**Open Graph Metadata:**
- Primary image: `https://jsinterview.dev/og-image.png` (1200x630)
- Type: website
- Locale: en_US
- Used for social media sharing previews

**Meta Tags:**
- Title: "JS Interview Prep - Master JavaScript with Interactive Code Visualization"
- Description: Includes keywords for interview prep, JavaScript concepts, and coding challenges
- Keywords: Explicit list covering interview questions, visualization, algorithms
- Robots: `index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1`

## Cross-Origin & Security

**Font CDN:**
- Preconnect to `https://fonts.googleapis.com` for performance
- No sensitive data transmitted

**Analytics Script:**
- Cross-origin tracking via Google Tag Manager
- Uses standard GTM integration pattern
- No personal data beyond event tracking

## Network Requests

**Only Outbound Requests:**
1. Google Analytics tag manager script load
2. Google Analytics page view/event tracking
3. Font CDN resources (preconnected)

**No Inbound API Calls:**
- Application is entirely client-side after initial page load
- No backend API consumed
- JavaScript code execution is simulated locally via custom interpreter

---

*Integration audit: 2026-01-22*
