---
phase: 15-responsive-implementation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DSAPatterns/TwoPointersViz/TwoPointersViz.module.css
  - src/components/DSAPatterns/HashMapViz/HashMapViz.module.css
  - src/components/DSAPatterns/BitManipulationViz/BitManipulationViz.module.css
  - src/components/Concepts/ArraysBasicsViz.module.css
  - src/components/Concepts/ObjectsBasicsViz.module.css
autonomous: true

must_haves:
  truths:
    - "Array visualizations scroll horizontally when content exceeds viewport width"
    - "Hash map bucket grid adapts to narrow screens without horizontal page scroll"
    - "Bit manipulation binary grid remains visible on mobile"
    - "No horizontal page scroll on any visualization at 320px width"
  artifacts:
    - path: "src/components/DSAPatterns/TwoPointersViz/TwoPointersViz.module.css"
      provides: "Responsive array container with horizontal scroll"
      contains: "overflow-x: auto"
    - path: "src/components/DSAPatterns/HashMapViz/HashMapViz.module.css"
      provides: "Responsive bucket grid"
      contains: "overflow-x: auto"
    - path: "src/components/DSAPatterns/BitManipulationViz/BitManipulationViz.module.css"
      provides: "Responsive binary grid"
      contains: "overflow-x: auto"
  key_links:
    - from: "Visualization containers"
      to: "Array/grid content"
      via: "overflow-x: auto with -webkit-overflow-scrolling: touch"
      pattern: "overflow-x:\\s*auto"
---

<objective>
Make visualizations adapt to narrow screens without causing horizontal page scroll.

Purpose: Visualizations with array displays or grids can exceed viewport width on mobile, causing awkward horizontal scroll of the entire page. This plan adds contained horizontal scroll to visualization containers.

Output: All visualization containers handle overflow gracefully with touch-friendly horizontal scroll, and content scales appropriately on mobile.
</objective>

<execution_context>
@/Users/sunnywilson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sunnywilson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-responsive-implementation/15-RESEARCH.md
@src/components/DSAPatterns/TwoPointersViz/TwoPointersViz.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make DSA array visualizations responsive</name>
  <files>
src/components/DSAPatterns/TwoPointersViz/TwoPointersViz.module.css
src/components/DSAPatterns/HashMapViz/HashMapViz.module.css
src/components/DSAPatterns/BitManipulationViz/BitManipulationViz.module.css
  </files>
  <action>
Update DSA pattern visualization CSS to handle array/grid overflow on narrow screens.

**TwoPointersViz.module.css:**

1. Update `.arrayContainer`:
   ```css
   .arrayContainer {
     display: flex;
     gap: var(--space-sm);
     justify-content: center;
     flex-wrap: nowrap; /* Change from wrap to nowrap */
     padding: var(--space-lg) var(--space-md);
     background: rgba(0, 0, 0, 0.4);
     border: 1px solid var(--js-viz-border);
     border-radius: var(--radius-xl);
     overflow-x: auto;
     -webkit-overflow-scrolling: touch;
     min-width: 0; /* Allow shrinking in flex context */
   }
   ```

2. Update mobile media query to handle cell sizing:
   ```css
   @media (max-width: 640px) {
     .cell {
       width: 36px;
       height: 36px;
       flex-shrink: 0;
     }

     .cellValue {
       font-size: var(--text-xs);
     }

     .arrayContainer {
       justify-content: flex-start;
       padding: var(--space-md) var(--space-sm);
     }
   }
   ```

**HashMapViz.module.css:**

1. Find the bucket grid container (likely `.bucketGrid` or similar) and add:
   ```css
   overflow-x: auto;
   -webkit-overflow-scrolling: touch;
   ```

2. Add mobile media query for bucket sizing:
   ```css
   @media (max-width: 640px) {
     .bucketGrid {
       gap: var(--space-xs);
     }

     /* If there are bucket cells, reduce their size */
     .bucket {
       min-width: 60px;
     }
   }
   ```

**BitManipulationViz.module.css:**

1. Find the binary display container and add:
   ```css
   overflow-x: auto;
   -webkit-overflow-scrolling: touch;
   ```

2. Add mobile media query for bit cells:
   ```css
   @media (max-width: 640px) {
     .bitCell {
       width: 28px;
       height: 28px;
       font-size: var(--text-2xs);
     }
   }
   ```

For each file:
- Identify the main visualization container that holds array/grid content
- Add `overflow-x: auto` and `-webkit-overflow-scrolling: touch`
- Add/update mobile media query (max-width: 640px) to reduce cell sizes
- Ensure content doesn't cause page-level horizontal scroll
  </action>
  <verify>
Run `npm run dev` and test each visualization page at 320px width:
1. TwoPointersViz: Array should scroll horizontally within its container
2. HashMapViz: Bucket grid should scroll if too wide
3. BitManipulationViz: Binary display should scroll if needed
No horizontal page scrollbar should appear.
  </verify>
  <done>
DSA pattern visualizations handle overflow with contained horizontal scroll, cells scale down on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make JS concept array visualizations responsive</name>
  <files>
src/components/Concepts/ArraysBasicsViz.module.css
src/components/Concepts/ObjectsBasicsViz.module.css
  </files>
  <action>
Update JS concept array/object visualizations for responsive display.

**ArraysBasicsViz.module.css:**

1. Find array display containers (likely `.stackSection`, `.heapSection`, or similar memory visualization) and add:
   ```css
   overflow-x: auto;
   -webkit-overflow-scrolling: touch;
   ```

2. Find array cell/element classes and ensure they have:
   ```css
   flex-shrink: 0; /* Prevent squishing */
   ```

3. Add mobile media query:
   ```css
   @media (max-width: 640px) {
     /* Reduce cell sizes for stack/heap visualization */
     .arrayCell, .cell, .element {
       min-width: 32px;
       font-size: var(--text-xs);
     }

     /* Container should scroll rather than wrap awkwardly */
     .arrayRow, .elementsRow {
       flex-wrap: nowrap;
       overflow-x: auto;
       -webkit-overflow-scrolling: touch;
     }
   }
   ```

**ObjectsBasicsViz.module.css:**

1. Same pattern - find object visualization containers and add overflow handling

2. Object property displays may need:
   ```css
   @media (max-width: 640px) {
     .propertyRow {
       flex-wrap: wrap; /* Allow property key-value to stack */
     }

     .objectBox {
       min-width: 0; /* Allow shrinking */
       overflow-x: auto;
     }
   }
   ```

Note: Read each file first to identify the exact class names used for array/object display containers, then apply the overflow-x: auto pattern appropriately.
  </action>
  <verify>
Run `npm run dev` and test:
1. ArraysBasicsViz at 320px: Stack/heap visualization scrolls horizontally if needed
2. ObjectsBasicsViz at 320px: Object displays don't cause page scroll
No page-level horizontal scrollbar.
  </verify>
  <done>
JS concept array and object visualizations handle narrow viewports with contained scroll.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Test at 320px viewport width (iPhone SE size):
   - TwoPointersViz: Array scrolls within container
   - HashMapViz: Bucket grid scrolls within container
   - BitManipulationViz: Binary grid scrolls within container
   - ArraysBasicsViz: Stack/heap area scrolls within container
   - ObjectsBasicsViz: Object visualization scrolls within container
3. No horizontal page scrollbar on any page at 320px width
4. Touch scrolling feels smooth (momentum scroll with -webkit-overflow-scrolling)
</verification>

<success_criteria>
- RESP-04 (responsive visualizations) satisfied: Visualizations adapt to screen width
- No horizontal page scroll at 320px minimum viewport
- Touch-friendly scroll with momentum (-webkit-overflow-scrolling: touch)
- Content remains readable with appropriately scaled cell sizes
</success_criteria>

<output>
After completion, create `.planning/phases/15-responsive-implementation/15-03-SUMMARY.md`
</output>
